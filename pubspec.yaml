name: responsive_builder2
description: A set of widgets that can be used to define a readable responsive UI for widgets.
version: 0.8.2
homepage: https://github.com/T-Pro/responsive_builder

funding:
  - https://ko-fi.com/filledstacks

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
